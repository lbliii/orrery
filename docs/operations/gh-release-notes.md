# GitHub Release Notes
`orrery/gh-release-notes` observes only the latest releases for Flask and the MCP Python SDK. Supply a prior `body_digest` from a previous receipt to get an unchanged/changed result; Orrery retains no baseline. GitHub API rate limits apply; calls are canonical HTTPS/no-redirect, bounded to 8 seconds/512 KiB.
