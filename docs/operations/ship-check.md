# Ship Check
`orrery/ship-check` combines named PyPI/npm release metadata, fixed Python release-note change evidence, and current UTC. It returns only `ready_to_reason` or `incomplete`; it never authorizes deployment and retains no baseline. Supply a prior source digest from an agent-held receipt for the source-watch comparison.
